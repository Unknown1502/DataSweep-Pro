node_modules/
dist/
dist-ssr/
*.local
.env
.env.local
.DS_Store
.vite/
coverage/
*.tsbuildinfo

# Demo recording pipeline — regenerate with demo/record.mjs (see docs/DEMO.md).
# Frames and the rendered video are large binaries; the API key must never ship.
demo/frames/
demo/*.mp4
demo/.env.local
